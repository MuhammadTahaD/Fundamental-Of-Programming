#include <iostream>
#include <conio.h>
#include <string>

using namespace std;
void insertElement(int*& arrays, int& size, int element, int position);
void deleteElement(int*& arrays, int& size, int element, int position);

int main()
{
    
}